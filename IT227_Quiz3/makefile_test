BUILDDIR = ${HOME}/Proj ${HOME}/Proj/bin ${HOME}/Proj/src ${HOME}/Proj/include ${HOME}/Proj/src/obj ${HOME}/Proj/lib
COPY_FILES = ./*.c

all: $(BUILDDIR) copy

$(BUILDDIR):
	mkdir -pv $@

copy:
	cp -fv $(COPY_FILES) ${HOME}/Proj/src

copy:
	cp -fv ./*.h ${HOME}/Proj/include